services:
  - type: web
    name: cateringboys0
    env: static
    staticPublishPath: dist  # Adjust to your build directory
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
