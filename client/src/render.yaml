services:
  - type: web
    name: your-service-name
    env: node
    buildCommand: npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
