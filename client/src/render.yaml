services:
  - type: web
    name: your-service-name
    env: static
    staticPublishPath: dist  # Adjust to your build directory
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
